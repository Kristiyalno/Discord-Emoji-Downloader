# Emoji Downloader

A small desktop UI for downloading Discord emojis from CDN links.

## Usage

```
python emoji_downloader.py
```

Paste one or more Discord emoji URLs into the text box, pick a save folder, and hit Download. Animated emojis are saved as `.gif`, static ones as `.webp`.

## Requirements

Python 3.x, no external dependencies.
